

<h1><%= @picture.title %>
  <small> by <%= @picture.artist %> </small>
</h1>
<%= image_tag(@picture.url) %>

<%= link_to("Back to Pictures", '/pictures') %> | <%= link_to("Edit", edit_picture_path(@picture)) %> | <%= link_to('Delete', "/pictures/#{@picture.id}", method: :delete, data: {confirm: "Are you sure you want to delete #{@picture.title}?"}) %>
